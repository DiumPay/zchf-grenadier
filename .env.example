# Optional. NEAR 1Click API JWT for the swap proxy at POST /near/quote.
# Without this set, the proxy route returns 404 and the rest of the API works as normal.
# Apply at https://partners.near-intents.org
NEAR_JWT=